#!/bin/bash
XBACKLIGHT_CFG="${HOME}/.xbacklight.conf"
if [[ $# -eq 0 ]]; then
	if [[ -s "$XBACKLIGHT_CFG" ]]; then
		xbacklight -set "$(sed -n '1p' "$XBACKLIGHT_CFG")"
	else
		xbacklight -get
	fi
else
	xbacklight "$@" && xbacklight -get > "$XBACKLIGHT_CFG"
fi
