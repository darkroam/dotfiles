#!/bin/sh

# entr command to run `queueandnotify` when newsboat queue is changed

[ "$(pgrep -x "$(basename "$0")" | wc -l)" -gt 1 ] && exit

printf '%s\n' "${XDG_DATA_HOME:-$HOME/.local/share}/newsboat/queue" | entr -p queueandnotify 2>/dev/null
